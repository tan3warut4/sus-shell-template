module.exports = {
  // Use the conventional commits configuration
  extends: ['@commitlint/config-conventional'],
  // You can add any custom rules or overrides here
  rules: {
    // For example, to enforce a specific case for the scope:
    // 'scope-case': [2, 'always', ['lower-case']],
  },
};
